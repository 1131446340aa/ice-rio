export interface IRequestServer {
    [key: string]: any;
}
export declare class CreateRequestServer {
    config: {
        baseUrl: string;
        timeout?: number;
        ts?: boolean;
    };
    socket: WebSocket;
    server: IRequestServer;
    constructor(config: {
        baseUrl: string;
        timeout?: number;
        ts?: boolean;
    });
    connect(): Promise<string>;
    install(): Promise<IRequestServer>;
    generateTS(): Promise<string>;
    type2Interface(obj: any): string;
}
